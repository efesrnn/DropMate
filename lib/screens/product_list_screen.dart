// Ana ekran (urun listesi) - bu commit'te placeholder olarak baslar,
// sonraki commit'te grid ve filtreler ile doldurulacaktir.

import 'package:flutter/material.dart';

class ProductListScreen extends StatelessWidget {
  const ProductListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Ana Ekran - yapim asamasinda')),
    );
  }
}
